.MODEL SMALL
.STACK 100H
.DATA
ARR DB 5, 9, 2, 15, 7

.CODE
MAIN PROC
    MOV SI, 0
    MOV AL, ARR[SI]

    MOV CX, 5

CHECK:
    CMP AL, ARR[SI]
    JGE NEXT
    MOV AL, ARR[SI]

NEXT:
    INC SI
    LOOP CHECK

    MOV AH, 4CH
    INT 21H
MAIN ENDP
END MAIN